from typing import List


class Solution:
    # Time: O(L * U) -> O(L)
    # Space: O(U) -> O(1)
    # Where:
    # L = len(tasks)
    # N = n
    # U = len(set(tasks))
    def leastInterval(self, tasks: List[str], n: int) -> int:
        # TODO: Solve using mathematical trick
        pass
