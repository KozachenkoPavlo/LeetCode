from typing import List


class Solution:
    # Time: O(N)
    # Space: O(1), don't count the result, since it is mandatory
    def summaryRanges(self, nums: List[int]) -> List[str]:
        # TODO: Solve this using strategy two pointers
        pass
